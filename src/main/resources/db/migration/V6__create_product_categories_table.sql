CREATE TABLE product_categories (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   product_id BIGINT,
   category_id BIGINT,
   created_at TIMESTAMP WITHOUT TIME ZONE,
   updated_at TIMESTAMP WITHOUT TIME ZONE,
   CONSTRAINT pk_product_categories PRIMARY KEY (id)
);

ALTER TABLE product_categories ADD CONSTRAINT FK_PRODUCT_CATEGORIES_ON_CATEGORY FOREIGN KEY (category_id) REFERENCES categories (id);

ALTER TABLE product_categories ADD CONSTRAINT FK_PRODUCT_CATEGORIES_ON_PRODUCT FOREIGN KEY (product_id) REFERENCES products (id);